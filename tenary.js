let x = 12;
let  number_type = x % 2 === 0 ? "Even" : "Odd";
console.log(number_type);

let y = -5;
let sign = y > 0 ? "Positive" : "Negative"
console.log(sign);

let s = 75;
let status = s >= 50 ? "Pass" : "Fail";
console.log(status);

let str = "Hello World";
let length = str.length > 10 ? "Long" : "Short";
console.log(length);

let p = 10, q = 10;
let Equal = p === q ? "yes" : "No"
console.log(Equal);